import 'package:flutter/material.dart';

class DripModel {
  String link;
  String title;
  String description;
  DateTime dateTime;
  Image image;

  DripModel({
    this.link,
    this.title,
    this.description,
    this.dateTime,
    this.image
  });
}